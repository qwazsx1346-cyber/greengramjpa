<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feedcomment.FeedCommentMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO feed_comment
        SET feed_id = #{feedId}
        , user_id = #{signedUserId}
        , comment = #{comment}
    </insert>

    <select id="findAll">
        SELECT fc.id, fc.feed_id,
        fc.`comment`, fc.created_at, u.id AS writerUserId,
        u.nm AS writerNm, u.pic AS writerPic
        FROM user u
        JOIN feed_comment fc
        ON fc.user_id = u.id
        WHERE fc.feed_id = #{feedId}
        ORDER BY fc.id DESC
        LIMIT #{startIdx}, #{size}
    </select>

    <delete id="deleteFeedComments">
        DELETE FROM feed_comment
        WHERE id = #{feedCommentId}
        AND user_id = #{signedUserId}
    </delete>

</mapper>